#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]
args.remote_email = "relay@example.com"
args.remote_host = "imap.example.com"
args.remote_password = "Passw0rd-for-tests"
